import dashboard from './dashboard';
// import payments from './payments';
// import transactions from './transactions';
// import reports from './reports';

// combine into a single menu object
const menuItems = {
  items: [dashboard]
};

export default menuItems;
